# Abonnement & Facturation + Paiement Stripe — Banani → Next.js 16 / Tailwind v4

## Source
- Banani flow `2oB_n5kLBeuy` (« Separate Screen Regen »)
  - `KM_nZ7xzMgAE` — **Subscription Plans** (page Abonnement in-app, thème « Gold Pro »)
  - `2Pl77h7xYQk2` — **Stripe Checkout** (page de paiement)
- Fetched: 2026-08-18
- Référence métier : `ekolplus/backend/src/modules/billing/*` (abonnement Stripe des écoles, plan Pro, checkout/portal/webhook + cron de sièges) — helpers repris : taux 0,60 $, annuel −10 % (Price distinct), essai 30 j, plafond Starter 50 (dur) / Pro 1000 (souple), cancel-at-period-end + resume in-app, changement d'intervalle proratisé, sync quotidien source de vérité = Stripe.
- Décisions utilisateur (2026-08-18) : Stripe = facturation SaaS uniquement ; prix = landing (Starter gratuit / Pro 0,60 $ / Enterprise devis) ; **annuel −10 % oui** ; **blocage Starter à 50 élèves oui (402)** ; **page récap + Stripe Checkout hébergé** (pas d'Elements in-app).

## Structure map — Subscription Plans → `/abonnement` (`components/school/billing/AbonnementScreen.tsx`)
- **2026-08-18 (2ᵉ passe)** : la page a quitté Paramètres — route dédiée `app/(school)/abonnement/page.tsx` (`max-w-6xl`), entrée sidebar `Compte › Abonnement` → `/abonnement`, checkout déplacé en `/abonnement/paiement`, redirections `next.config.ts` (`/settings?tab=subscription`, `/settings/abonnement/*`), URLs Stripe (success/cancel/portal) mises à jour.
- `page-header` : titre 22px/800 −0.4px + sous-titre 13px ; droite : chip warning « Renouvellement dans N jours » (12px/600, 7×12) + bouton primaire « Mettre à niveau » (13px/600, 7×16) → `CurrentPlanBanner` action, `Button size=sm`.
- `.current-plan-banner` (18×24, radius-lg) : icône 44px bg-secondary · « PLAN ACTUEL » 10px/600 0.8px · nom 20px/800 primary · sous 11px · divider 1×44 · stats 22px/800 + 11px (Élèves actifs / Enseignants / Classes / Admins) ; droite : badge success 12px/600, « Expire le … » 11px, « Gérer le plan » (secondary 6×12 11px/600) → `components/school/billing/CurrentPlanBanner.tsx`.
- `.section-card` Utilisation du plan : header 13×18 (13px/700 + 12px), grille 4 cellules 16×18 border-r : label 10px/700 0.7px, used 22px/800 + total 13px/500, barre 5px (ok = success-foreground, warn ≥ 80 % = warning-foreground), hint 10px → `UsageCard.tsx`. Cellules réelles : Élèves (plafond dur 50 / souple 1000), Enseignants / Classes / Administrateurs « Illimité » (pas de stockage ni de compteur de bulletins dans le modèle).
- « Choisir un plan » : titre 15px/800 + sous 12px ; `.billing-toggle` (pill muted p3, options 5×16 12px/600, active card + shadow 0 1px 4px .09) + `.save-badge` success 10px/700 → `BillingCycleToggle.tsx` (affiché seulement si `STRIPE_PRICE_ID_PRO_ANNUAL`).
- `.plans-grid` 3 col gap 16 → `PlanCards.tsx` : `.plan-card` 22/20/18 gap 14 radius-lg ; popular = border primary + ring 2px primary/10 + badge −11px 10px/700 ; icône 40px radius-md ; nom 16px/800 ; tagline 11px ; prix 32px/800 + devise 15px/700 + période 12px ; per-pupil 11px ; divider ; features 12px gap 8 (Check success / X muted) ; CTA 10×14 13px/600 (outline / primary / current = secondary) ; `.stripe-badge` #635bff 10px/800 sous Pro.
- `.bottom-row` 1fr/2fr gap 16 (lg ; empilé en dessous) : Moyen de paiement (`PaymentMethodCard.tsx` — icône 40×27 bg-foreground, nom 13px/600, sous 11px, bouton « Modifier » 5×10 11px, strips success + info #f5f3ff/#c4b5fd) · Historique de facturation (`BillingHistoryTable.tsx` — th 10×16 11px/700 0.5px bg-background, td 11×16 13px, `.status-badge` 3×9 11px/600, `.dl-btn` secondary 4×10 11px « PDF » → `hosted_invoice_url`, « Tout exporter » CSV).
- Bloc « Gérer votre abonnement » (hors mock, repris d'ekolplus) : Moyen de paiement & factures (portal) · Passer à l'annuel / Repasser au mensuel · Annuler l'abonnement / Reprendre — chacun avec confirmation inline.

## Structure map — Stripe Checkout → `abonnement/paiement/page.tsx`
- Topbar standalone du mock → `PageHeaderCard` (← Abonnement · chip cadenas · « Paiement sécurisé » · meta « Abonnement › Établissement Pro » · badge success « Connexion SSL 256-bit »).
- Étapes (22px cercles, 12px libellés, connecteurs 24px) → `FormStepsBar` existant (Choisir le plan ✓ · Paiement · Confirmation).
- `.checkout-body` 440 + 340 sticky → **`ASIDE_GRID`** (règle app : colonne droite 360/420, `lg:sticky lg:top-4`).
- Cartes gauche = `FormSectionCard` (header 16×20 icône 30px bg-secondary, titre 14px/700, sous 12px ; body 20) :
  1. Sélectionner le plan — `.plan-option` 12×14 border 1.5px (selected = primary + #faf7ff, radio 16px), Starter « Plan actuel » non sélectionnable, Pro sélectionné ; cycle = 2 tuiles (10×14, active border 2px primary + #faf7ff, badge success « Économisez 10 % » −10px) ; effectif = compteur 22px/800 primary en lecture seule (sièges = effectifs, sync quotidien) + `.count-estimate` bg-secondary 10×14 (12px/600 label, 15px/800 valeur).
  2. Informations de facturation — 6 `ReadField` (form-input 9×12 border 1.5px min-h 38px, 13px) lecture seule depuis `/api/school` (nom, code officiel/NIF, email, téléphone, pays, adresse) + lien Paramètres › Établissement.
  3. Paiement par carte — chips VISA/MC/AMEX 32×20 + strip #f5f3ff « Paiement traité par Stripe » ; **pas de champs carte** (saisie sur la page Stripe).
- Droite : `.summary-card` (header gradient 130° #6c2bd9→#9b52f5 14×18, 13px/700 blanc + 11px .75 ; body 16×18 gap 10 rows 12px ; total 14px/700 + 20px/800 primary ; strip économie success ; strip essai 30 j) ; features 14×18 border-t 12px ; `.pay-btn` 14px/700 h-12 → POST `/api/school/billing/checkout` → redirect ; `.pay-btn-sub` 11px ; `.stripe-powered` #f5f3ff ; notes muted 12×14 (essai / résiliation / support) ; `.security-row` gap 20 11px.
- États : `?annule=1` (strip warning), `?etape=confirmation` (poll `GET /api/school/billing` toutes les 2 s ×10 jusqu'à `managedByStripe`), non-OWNER (bouton désactivé + note), déjà Pro (strip + bouton désactivé), Stripe non configuré (503 → toast).

## Données / modèle (migration 28 `stripe_billing`)
- `SubscriptionPlan.key` renommé EN PLACE : ESSENTIEL→PRO (60 ¢, « Établissement Pro »), PREMIUM→ENTERPRISE, STARTER→0 ¢ (FK `Subscription.planId` `onDelete: Restrict` — jamais delete+recreate).
- `School.stripeCustomerId` (unique) ; `Subscription.stripeSubscriptionId` (unique) / `stripeSubscriptionItemId` / `stripeStatus` / `billingInterval` (enum MONTH|YEAR) / `cancelAtPeriodEnd` / `billedSeats` ; `BillingTransaction.stripeInvoiceId` (unique) / `stripePaymentIntentId` (unique) / `stripeInvoiceUrl`.
- Serveur : `lib/billing-plans.ts` (catalogue partagé client/serveur), `lib/server/billing/{stripe-client,stripe,summary,route-guards}.ts`, `lib/server/webhook/stripe.ts`, routes `api/school/billing/{,checkout,portal,subscription}`, `api/webhooks/stripe`, `api/cron/stripe-sync`, blocage 402 `PLAN_LIMIT_REACHED` dans `POST /api/school/students`, `scripts/stripe-setup-prices.ts`.
- Env : `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_PRICE_ID_PRO`, `STRIPE_PRICE_ID_PRO_ANNUAL` (optionnel).

## Responsive
- 375 : bannière plan empilée (stats 2×2), usage 2 col, cartes plan 1 col, bottom-row empilé, tableau défile en x (`min-w-[560px]`) ; checkout : une colonne, résumé sous les cartes, bouton pleine largeur.
- 768 (`md`) : cartes plan 3 col ; usage 2 col.
- 1024 (`lg`) : usage 4 col, bottom-row 1fr/2fr, checkout `ASIDE_GRID` (colonne droite 360 sticky) ; 1280 (`xl`) : colonne droite 420.

## Checklist
- [x] Schéma + migration 28, seed plans réconcilié, littéraux ESSENTIEL/PREMIUM remplacés (admin subscriptions/coupons/schools, `planEmoji`, seed-dev-school)
- [x] SDK `stripe` épinglé (`apiVersion` 2026-07-29.dahlia), client paresseux (absent = 503 / no-op)
- [x] Webhook `POST /api/webhooks/stripe` via la factory PROTÉGÉE (event.id dedup, `kind` = slot de dispatch)
- [x] Routes école GET / checkout / portal / PATCH subscription (OWNER-only mutations)
- [x] Cron `stripe-sync` (vercel.json + tripwire 8 crons + CLAUDE.md)
- [x] Blocage 402 Starter > 50 élèves
- [x] AbonnementTab (Subscription Plans) + page `/settings/abonnement/paiement` (Stripe Checkout) mobile-first
- [x] Tests : 62 nouveaux (provider, sync/invoice/refund/checkout, routes, cron, summary/limites) — 1115/1115 verts ; `format` / `lint` / `typecheck` verts
- [x] Vérification 375 / 768 / 1280 sur le dev server (Playwright, `scratchpad/verify-billing.mjs`) — 768/1280 fidèles au mock ; 375 : correctifs appliqués (prix des options de plan empilé, `min-w-0` sur les champs lecture seule, valeur de l'estimation `whitespace-nowrap`, colonne Période `whitespace-nowrap`, action de la bannière rendue directement) — re-capture 375 après patch non relancée (session interrompue), à refaire au besoin
- [x] Bout-en-bout réel avec clés Stripe test — 2026-08-18, compte `acct_1TxoYwFtbBjU6nXJ` (mode Test), école dev (42 élèves) : récap → Checkout hébergé (carte 4242, « Démarrer la période d'essai ») → retour `?etape=confirmation` → webhooks (`stripe listen --api-key`) → Subscription TRIAL/`trialing`, 42 sièges, client Stripe créé ; cron `stripe-sync` réaligne 40 → 42 ; PATCH annulation programmée → reprise, MONTH → YEAR → MONTH ; portail `billing.stripe.com` OK ; 2ᵉ checkout → 409 `ALREADY_SUBSCRIBED` ; `stripe trigger invoice.payment_failed` étranger → 200 + warn « non corrélé » ; 17 webhooks 200. **Un 500** au tout premier rejeu (`checkout.session.completed` concurrent à 2 autres webhooks + polling) : « Unable to start a transaction in the given time » — la `DATABASE_URL` dev a `connection_limit=1` ; idempotent, Stripe rejoue en prod — envisager `connection_limit≥5` sur l'URL poolée.
- [x] 2ᵉ passe 2026-08-18 : page `/abonnement` + thème or + carte sidebar — 1142/1142 tests (plan-presentation ×15, snapshot ×4, route plan ×2, route-match/breadcrumb, retry webhook ×2) ; vérifié Playwright 1280/375 + rail réduit + drawer, redirections OK, transition réelle résiliation → réactivation (checkout sans essai, 25,20 $ test) avec sidebar synchronisée sans rechargement.
- Robustesse webhooks (2ᵉ passe) : (1) `.env.local` dev `connection_limit=1 → 5` (un seul process ; avec 1, les 3 événements simultanés d'un checkout ne pouvaient pas ouvrir leur transaction en 2 s) — garder 1 sur Vercel ; (2) `POST /api/webhooks/stripe` rejoue UNE fois un 500 (clone de la requête avant lecture du corps) : sous `Serializable`, Postgres abandonne l'une des transactions concurrentes (P2034 « write conflict ») — c'est attendu, la consigne est retry ; Stripe reste le filet en prod, `stripe listen` ne rejouant jamais.
- Piège rencontré : la CLI Stripe était connectée à un *sandbox* (`acct_1TxoZAFwP604RaL1`) ≠ compte de la `sk_test_` → `stripe listen` n'a rien reçu ; solution : `stripe listen --api-key <sk_test> …` (le `whsec_` change avec le compte).

## 3ᵉ passe 2026-08-18 — « prête pour la production » (sélection de plan, transitions, robustesse, rôles)
Décisions utilisateur : **pas de verrouillage de modules** (les plans diffèrent par le plafond d'élèves + le support) ; **impayé → retombe en Starter automatiquement**.
- **Cartes sélectionnables** — `PlanCards.tsx` = `role="radiogroup"` ; chaque carte est un `role="radio"` (clic n'importe où, ←/→ + Espace/Entrée, tabindex tournant, anneau or pour Pro / `primary` pour Starter-Enterprise, pastille radio en haut à droite, chip « Plan actuel »). Le CTA de CHAQUE carte est un vrai `<button>` (stopPropagation) piloté par la matrice de transitions ; sous la grille, `PlanTransitionHint` (`aria-live`) explique la carte sélectionnée. Deep link `?plan=PRO` (carte sidebar « Découvrir » / « Réactiver » / « Régulariser ») présélectionne Pro et fait défiler jusqu'à `#plans`.
- **Matrice de transitions** — `plan-transition.ts` (`planTransition({selected, billing, canManage, interval})`, 25 tests) : `current` · `resume` (Pro résiliation programmée → « Reprendre ») · `upgrade` (« Passer à Pro — essai 30 j », estimation selon le cycle) · `reactivate` (ex-Pro Stripe → sans essai) · `regularize` (Pro SUSPENDED/impayé → portail « Régulariser le paiement ») · `downgrade` (« Rétrograder vers Starter » → `DowngradeDialog` : date d'effet, aucun remboursement, plafond 50 avec l'effectif réel, données/modules conservés, reprise possible → PATCH `cancelAtPeriodEnd:true`) · `downgrade-scheduled` (« Annuler la rétrogradation ») · `contact` (Enterprise « Demander un devis » = `mailto:` `NEXT_PUBLIC_SALES_EMAIL` sujet+corps préremplis école/plan/effectif ; Pro suspendu manuellement) · `managed` (contrat Enterprise / Pro manuel → CTA désactivé) · `unavailable` (Stripe absent). ADMIN (non-OWNER) : même transition, CTA désactivé « Réservé au propriétaire de l'établissement » (le devis reste ouvert).
- **Vocabulaire unique** — « Annuler l'abonnement » devient partout « Rétrograder vers Starter » ; bannière « Rétrogradation vers Starter programmée le … » avec bouton « Annuler la rétrogradation » (or) ; toasts alignés.
- **Impayé → Starter** — `effectivePlanKey` : `SUSPENDED` rejoint CANCELED/EXPIRED (plan effectif STARTER, `subscribedPlan` reste PRO) → plafond 50 réappliqué (`checkStudentLimit`), bannière « Starter — Établissement Pro suspendu · plafond 50 élèves » + pill « Impayé » + bouton or « Mettre à jour la carte » (portail), alerte destructive, sidebar `regularize` (« Établissement Pro suspendu / ⚠ Paiement en échec — régulariser / Régulariser »), carte Pro « Régulariser le paiement ». Suspension back-office (sans Stripe) → « Suspendu · contactez-nous ».
- **Anti-double abonnement** — `createCheckoutSession` : `findOpenSubscription(customer)` côté Stripe (`trialing|active|past_due|unpaid|paused|incomplete`) → sync de la ligne + `AlreadySubscribedError` → 409 `ALREADY_SUBSCRIBED` ; webhook `checkout.session.completed` → `reconcileCheckoutSubscription` annule sur Stripe (`prorate:true`) un ancien abonnement encore ouvert de la même école (le plus récent gagne, jamais facturée deux fois) ; cron `stripe-sync` → `log.error` si > 1 abonnement ouvert par client (Sentry).
- **Rôles** — `GET /api/school/billing` → OWNER/ADMIN (`requireSchoolBilling(…, 'ADMIN')`), MEMBER 403 → page « Réservé aux administrateurs de l'établissement » (`useBilling.errorCode`) ; `GET /api/school/billing/plan` → MEMBER `{ plan: null }` → pas de carte sidebar ; `NavItem.minRole: 'ADMIN'` + `filterSectionsByRole` (5 tests) masque « Abonnement » pour un MEMBER.
- **Fin d'essai & emails** — `customer.subscription.trial_will_end` → `handleTrialWillEnd` (sync + notification in-app `subscriptionTrialEnding` OWNER/ADMIN, dedupe `subscription-trial-ending:${sub}:${user}`, estimation = quantité × prix) ; `syncCustomerEmail` (cron) réaligne `customer.email` sur l'email du OWNER courant → les emails Stripe (reçus, échec, fin d'essai) partent à la bonne adresse.
- **Cartes honnêtes** — `PLAN_FEATURES` sans ✗ sur un module réellement disponible (Starter : jusqu'à 50 élèves, tous les modules, gratuit ; ✗ seulement « Au-delà de 50 élèves » et « Support prioritaire »).
- **Récap paiement** — options de plan = vrais `<input type="radio" name="plan">` (Starter désactivé + « Plan actuel » ou « plan de repli », Pro coché) ; « conditions d'utilisation » → lien `NEXT_PUBLIC_TERMS_URL` (défaut `/cgu` — **page à créer**, aujourd'hui la landing pointe `#`).
- **Outillage** — `scripts/stripe-doctor.ts` (`pnpm stripe:doctor` / `stripe:doctor:live`, 10 tests, lecture seule) : mode test/live, compte (`charges_enabled`, descripteur), Prices (existence, `licensed`, USD, montants = `billing-plans.ts`), portail (carte + factures + résiliation ON, changement de plan OFF), endpoint webhook `<APP_URL>/api/webhooks/stripe` avec les 8 événements (`lib/stripe-events.ts`), `STRIPE_WEBHOOK_SECRET`, `NEXT_PUBLIC_SALES_EMAIL` / `NEXT_PUBLIC_TERMS_URL` ; sortie 1 sur bloqueur. Passé en test le 2026-08-18 : ✓ Prices, ✓ portail, ! pas d'endpoint (normal en test), ! CGU.
- **Vérifié** — 1199/1199 tests, format/lint/typecheck verts ; Playwright (`scratchpad/verify-prod-ready.mjs`, 1280 + 375) : sélection clic/clavier + hint par carte, mailto Enterprise, rétrogradation réelle (dialogue → PATCH 200 → bannière + sidebar « Actif jusqu'au 18/09/2026 » + cartes « Annuler la rétrogradation » / « Reprendre ») puis reprise réelle (PATCH 200 → « Renouvellement 18/09/2026 »), récap radios + lien CGU, mocks MEMBER (page réservée, pas de carte ni d'entrée sidebar), SUSPENDED (bannière/CTA/sidebar/hint `regularize`), STARTER-ADMIN (CTA désactivé, `?plan=PRO` sélectionne + défile), STARTER-OWNER (upgrade + estimation annuelle, CTA → `/abonnement/paiement?cycle=YEAR`), 375 sans débordement (cartes + dialogue).

### Mise en prod Stripe — checklist (à cocher au lancement)
1. Vercel prod env : `STRIPE_SECRET_KEY` **live restreinte** (`rk_live_…` — droits : Customers, Checkout Sessions, Subscriptions, Prices, Billing Portal, Webhook Endpoints en lecture pour le doctor), `STRIPE_WEBHOOK_SECRET` live, `STRIPE_PRICE_ID_PRO` live (déjà créé), `STRIPE_PRICE_ID_PRO_ANNUAL` live (`pnpm stripe:setup-prices -- --annual-only` avec la clé live), `NEXT_PUBLIC_SALES_EMAIL`, `NEXT_PUBLIC_TERMS_URL`, `APP_URL`. Garder `connection_limit=1` sur la `DATABASE_URL` Vercel.
2. Dashboard → Développeurs → Webhooks : endpoint `https://<domaine>/api/webhooks/stripe`, les 8 événements de `lib/stripe-events.ts`, copier le `whsec_`.
3. Dashboard → Paramètres → Facturation → Abonnements & emails : **Smart Retries** activées, « annuler l'abonnement » après la dernière tentative (→ CANCELED → Starter, réactivation possible depuis l'app), emails Stripe activés (reçus, échec de paiement avec lien de mise à jour de carte hébergé, rappel de fin d'essai).
4. Dashboard → Paramètres → Portail client : moyen de paiement + historique des factures + résiliation activés, **changement de plan désactivé** (l'app est la seule UX de plan).
5. Dashboard → Paramètres → Informations publiques : descripteur de relevé « SCHOOLGESTI », email support.
6. Créer la page CGU (`/cgu`) ou pointer `NEXT_PUBLIC_TERMS_URL` vers les conditions hébergées ; confirmer `contact@schoolgesti.com`.
7. `pnpm stripe:doctor:live` (charge `.env.production.local`) → 0 bloqueur.
8. Stripe Tax laissé désactivé (décision à revoir avec l'expert-comptable : entité FR facturant en USD).
9. Test réel : une école de test en live avec un coupon 100 % → checkout → webhooks → cron → rétrogradation → reprise, puis annulation.
10. **Sièges = effectif réel** : vérifier sur le projet Vercel prod que `CRON_SECRET` est défini et que `vercel.json` a bien déployé le cron `stripe-sync` (03:00 UTC, quotidien) — c'est lui qui recompte les élèves de chaque école Pro chaque jour et aligne `quantity` sur l'abonnement Stripe (`syncSeatQuantity`, proration `none` → le prochain prélèvement mensuel/annuel suit l'effectif). Le webhook `invoice.upcoming` n'est pas utilisé ; sans ce cron, la quantité ne bouge qu'au checkout. Contrôle : Vercel → Cron Jobs → dernière exécution `200`, ou Dashboard Stripe → abonnement → quantité = nombre d'élèves.
11. **Coupons admin sur Checkout** : après `db:migrate:deploy` (migration 29), `pnpm stripe:sync-coupons:live` une fois pour mirrorer les coupons existants en Promotion Codes Stripe (les nouveaux le sont à la création). Voir `.planning/banani/admin-coupons.md` § Miroir Stripe.

## Thème « or » = plan payant (décision utilisateur 2026-08-18, exception explicite à « pas de couleurs par carte »)
- Tokens `--color-gold-100…900` dans `globals.css` (texte foncé sur or, AA) ; `Badge tone="gold"`, `Button variant="gold"` (dégradé 300→500, texte gold-900).
- **Sidebar** — `components/school/billing/SidebarPlanCard.tsx` dans le slot `footer` / `footerCollapsed` de `Sidebar` (au-dessus du profil) : Starter → carte dégradé or « Passez à Établissement Pro » + CTA « Découvrir » (sous-ligne d'urgence à ≥ 80 % / 100 % du plafond 50, « Réactivez » après un Pro résilié/expiré) ; Pro/Enterprise → bandeau or pâle « Établissement Pro · Essai jusqu'au … / Renouvellement … / Actif jusqu'au … / ⚠ Paiement en échec » cliquable → `/abonnement` ; rail réduit = pastille couronne + tooltip. Logique pure `plan-presentation.ts` (15 tests). Rien n'est affiché sans école, en chargement, ou en Starter sans Stripe.
- **État partagé** — `contexts/SchoolPlanContext.tsx` monté dans `(school)/layout.tsx` : `GET /api/school/billing/plan` (`getPlanSnapshot`, 2 requêtes) une fois par shell + revalidation ≤ 1/min à la navigation ; `useBilling` pousse (`apply`) chaque summary chargé/patché → la sidebar bascule dans la même seconde que la page (vérifié : résiliation → « Réactivez » ; nouveau checkout → « Renouvellement 18/09/2026 » sans rechargement).
- **Écrans** — carte Pro or (bordure gold-400 + halo, badge/icône/CTA dégradé, nom/prix/coches gold-700), Enterprise neutre (`Building2`, badge gris), bannière plan or (voile, couronne, pill PRO), alerte essai or, récap paiement : option Pro or + badge « Le plus populaire », tuiles de cycle or, en-tête résumé or, bouton payer or.
- Statuts `subscriptionStatusLabel` : ACTIVE/TRIAL d'un plan payant → tone `gold`.

## Écarts assumés vs mock
- Enterprise : carte blanche standard (badge muted « 🏆 Le plus complet », icône warning) au lieu du dégradé or — règle utilisateur « pas de couleurs par carte » (cards-redesign) et cohérence avec la landing.
- Moyen de paiement : pas de « Visa •••• 4821 » ni de MonCash — la carte n'est jamais stockée côté app ; « Modifier » ouvre le Customer Portal Stripe. MonCash/NatCash restent hors périmètre SaaS.
- Utilisation : Stockage / Bulletins générés remplacés par Classes / Administrateurs (seules métriques réelles) ; « Réinitialisation le … » remplacé par « N sièges facturés ».
- Checkout : aucun champ carte, ni code promo (saisi sur Stripe — `allow_promotion_codes`), ni sélecteur d'effectif (les sièges suivent les effectifs réels) ; colonne droite `ASIDE_GRID` et non 340px ; « Économisez 20 % » → 10 % (Price annuel réel) ; HTG retiré (facturation USD).
- Chip d'en-tête : « Renouvellement dans N jours » devient « Fin d'essai dans … » / « Fin d'accès dans … » selon l'état.
