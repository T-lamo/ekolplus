// Promoted to a ui/ primitive when the SaaS admin tables became the 3rd+
// consumer (see .planning/banani/epic-2-admin-foundation.md) — this
// re-export keeps the fee screens' existing imports working unchanged.
export { Pager } from '@/components/ui/Pager';
